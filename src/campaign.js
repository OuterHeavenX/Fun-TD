(()=>{
const overlay=document.getElementById('campaignOverlay'),grid=document.getElementById('sectorGrid'),open=document.getElementById('campaignButton'),close=document.getElementById('campaignClose');if(!overlay||!grid)return;
function migrateLegacy(n){const cleared=localStorage.getItem(`funTD_sector${n}Clear`)==='1';if(!cleared)return;const starsKey=`funTD_sector${n}Stars`,waveKey=`funTD_sector${n}BestWave`;if((+localStorage.getItem(starsKey)||0)===0)localStorage.setItem(starsKey,'1');if((+localStorage.getItem(waveKey)||0)<20)localStorage.setItem(waveKey,'20')}
[1,2,3].forEach(migrateLegacy);const unlocked2=()=>localStorage.getItem('funTD_sector1Clear')==='1',unlocked3=()=>localStorage.getItem('funTD_sector2Clear')==='1';const data=n=>({stars:+localStorage.getItem(`funTD_sector${n}Stars`)||0,bestHp:+localStorage.getItem(`funTD_sector${n}BestHp`)||0,bestWave:+localStorage.getItem(`funTD_sector${n}BestWave`)||0});const starRow=n=>{const s=data(n).stars;return `<div class="stars">${[1,2,3].map(i=>`<span class="${i<=s?'':'off'}">★</span>`).join('')}</div>`};function card(n,name,desc,locked=false,cls=''){const d=data(n);return `<article class="sector-card ${cls} ${locked?'locked':''}"><span class="sector-num">SECTOR ${String(n).padStart(2,'0')}</span><h3>${name}</h3><p>${desc}</p>${starRow(n)}<div class="sector-stats"><span>BEST WAVE ${d.bestWave||'—'}/20</span><span>BEST BASE ${d.bestHp?`${d.bestHp} HP`:'—'}</span></div><button data-sector="${n}" ${locked?'disabled':''}>${locked?'LOCKED':d.stars?'REDEPLOY':'DEPLOY'}</button></article>`}function render(){grid.innerHTML=card(1,'DUSTWALL DEFENSE','Hold the desert command line against the first swarm.',false,'dust')+card(2,'FROSTLINE PASS','Defend the frozen corridor through whiteout conditions.',!unlocked2(),'frost')+card(3,'ASHFALL FOUNDRY','Defend the volcanic foundry against fragmenting Splitters.',!unlocked3(),'ash')+`<article class="sector-card locked"><span class="sector-num">SECTOR 04</span><h3>CLASSIFIED</h3><p>Signal unavailable. Further operations remain encrypted.</p><div class="stars"><span class="off">★</span><span class="off">★</span><span class="off">★</span></div><div class="sector-stats"><span>COMING SOON</span></div><button disabled>LOCKED</button></article>`;grid.querySelectorAll('[data-sector]').forEach(b=>b.onclick=()=>{const n=b.dataset.sector;location.href=location.pathname+(n==='1'?'':`?sector=${n}`)})}function show(){render();overlay.classList.add('show')}function hide(){overlay.classList.remove('show')}open?.addEventListener('click',show);close?.addEventListener('click',hide);overlay.addEventListener('pointerdown',e=>{if(e.target===overlay)hide()});})();