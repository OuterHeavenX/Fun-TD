export const selectTarget=(tower,enemies,mode='first')=>{const valid=enemies.filter(e=>e.active&&e.hp>0&&Math.hypot(e.x-tower.x,e.y-tower.y)<=tower.range);if(!valid.length)return null;const cmp={first:(a,b)=>b.distance-a.distance,last:(a,b)=>a.distance-b.distance,strongest:(a,b)=>b.hp-a.hp,weakest:(a,b)=>a.hp-b.hp,closest:(a,b)=>Math.hypot(a.x-tower.x,a.y-tower.y)-Math.hypot(b.x-tower.x,b.y-tower.y)}[mode];return valid.sort(cmp)[0]};
