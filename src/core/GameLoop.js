export class GameLoop{constructor(update,render){this.update=update;this.render=render;this.running=false;this.last=0;this.acc=0;this.raf=0;this.tick=this.tick.bind(this)}start(){if(this.running)return;this.running=true;this.last=performance.now();this.raf=requestAnimationFrame(this.tick)}tick(now){if(!this.running)return;let dt=Math.min(.1,(now-this.last)/1000);this.last=now;this.acc+=dt;while(this.acc>=1/60){this.update(1/60);this.acc-=1/60}this.render(this.acc/(1/60));this.raf=requestAnimationFrame(this.tick)}stop(){this.running=false;cancelAnimationFrame(this.raf)}}
