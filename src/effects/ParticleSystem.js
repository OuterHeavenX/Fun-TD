export class ParticleSystem{constructor(max=520){this.max=max;this.items=[]}burst(x,y,color,n=6,power=80){for(let i=0;i<n&&this.items.length<this.max;i++)this.items.push({x,y,vx:(Math.random()-.5)*power,vy:(Math.random()-.8)*power,life:.35+Math.random()*.4,max:.75,color,size:2+Math.random()*4})}update(dt){for(let i=this.items.length-1;i>=0;i--){const p=this.items[i];p.life-=dt;if(p.life<=0){this.items.splice(i,1);continue}p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=90*dt}}draw(c){for(const p of this.items){c.globalAlpha=Math.max(0,p.life/p.max);c.fillStyle=p.color;c.beginPath();c.arc(p.x,p.y,p.size,0,7);c.fill()}c.globalAlpha=1}clear(){this.items.length=0}}
