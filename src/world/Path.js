export class Path{constructor(points){this.points=points;this.seg=[];this.total=0;for(let i=0;i<points.length-1;i++){const a=points[i],b=points[i+1],l=Math.hypot(b[0]-a[0],b[1]-a[1]);this.seg.push({a,b,l,start:this.total});this.total+=l}}at(distance,out={x:0,y:0,angle:0}){const d=Math.max(0,Math.min(this.total,distance));let s=this.seg[this.seg.length-1];for(const q of this.seg)if(d<=q.start+q.l){s=q;break}const t=Math.max(0,Math.min(1,(d-s.start)/s.l));out.x=s.a[0]+(s.b[0]-s.a[0])*t;out.y=s.a[1]+(s.b[1]-s.a[1])*t;out.angle=Math.atan2(s.b[1]-s.a[1],s.b[0]-s.a[0]);return out}}
