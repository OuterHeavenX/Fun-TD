import test from'node:test';import assert from'node:assert/strict';import{upgradeCost,refundFor,damageAfterArmor}from'../src/data/balance.js';test('upgrade prices escalate deterministically',()=>{assert.equal(upgradeCost(100,1),160);assert.equal(upgradeCost(100,4),480)});test('refund is configured percentage',()=>assert.equal(refundFor(100),68));test('armor and piercing',()=>{assert.equal(damageAfterArmor(100,.4),60);assert.equal(damageAfterArmor(100,.4,.2),80)});
