import test from'node:test';import assert from'node:assert/strict';import{selectTarget}from'../src/combat/TargetingSystem.js';const tower={x:0,y:0,range:100},a={active:true,hp:5,x:10,y:0,distance:20},b={active:true,hp:20,x:20,y:0,distance:40};test('target modes select correctly',()=>{assert.equal(selectTarget(tower,[a,b],'first'),b);assert.equal(selectTarget(tower,[a,b],'weakest'),a);assert.equal(selectTarget(tower,[a,b],'closest'),a)});
